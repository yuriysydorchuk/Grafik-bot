# HANDOFF: сводні — премія/бонус готівкою, борг у наступний місяць, колонки (20.08.2026)

Сесія 20.08.2026. Один батч по сводних, задеплоєний того ж дня. Довговічні правила вже перенесені в `PROJECT_MAP.md` (секція «Сводні»); тут — конспект сесії.

## Що зроблено

1. **Колонки сводної — точковий показ.** Клік по чіпу колонки, схованої тумблером («Без порожніх колонок» / «Księgowe: сховано»), показує лише її (маркер ✦), тумблер не вимикається. Новий localStorage-набір `svodni.shownCols.<місяць>.<місто>.<фабрика>` поруч із `hiddenCols`; підсумкова видимість рахується одним місцем у `Svodni.tsx` і передається у `FactoryTable` готовим набором.

2. **Agram (12 Motycz / 13 Lublin): колонка Premia — завжди готівкою.** `PREMIA_CASH_FACTORY_IDS` у `services/svodni.ts`; `applyLegalDefaults` зменшує стелю конто на премію в усіх гілках (вкл. побажання «все на конто» і ручну суму). Виняток — студент до 26: уся виплата з премією на конто. Детекція по `LegalCtx.factoryId` (прокинуто в усі виклики); легасі-рядки без `factory_id` — стара поведінка.

3. **Вшитий бонус (нал/стаж Agram + нал LST) — завжди готівкою.** Рядок/сегмент несе `extras.facBonus` (зл/год); `ksiegRatesOf` віднімає його від ставки ДО клампу 25,35, стеля конто зменшується на суму бонусу. Закриті діри: база нижча за стандартну, «все на конто», хибний «студентський» детект бонусної нетто > брутто. Пишуть facBonus: from-hours, перерахунки з профілю, сегментний двигун (посегментно — `SegState/SegmentCalcIn.facBonus`), «Додати людину», unsplit, лок-фриз віку.

4. **Мінусова виплата = борг.** (а) «Разом» унизу вкладки і Razem Excel сумують До виплати/Готівку/Конто лише по додатних рядках. (б) from-hours авто-переносить мінус M−1 у **ту ж колонку** рядка M: «черга знять» `DEBT_DEDUCTION_ORDER` — аванси знімаються першими, недозняте лягає в останні (вибір Юрія); ідемпотентно; мінус виправили → борг і маркери зникають. Маркери `extras.debtIn`/`debtOut` (бейджі ⤵/⤴), звіт `debtCarried`/`debtUnmatched` у тості; транспорт/одяг-переноси зберігають борг-частину Dojazd/Odzież.

Разом поїхав **Gratyfikant-експорт (веб-частина)** — модалка на /svodni + `services/gratyfikantExport.ts` (бекенд-роут уже був у main); чекає зразка від księgowej для звірки формату.

## Стан деплою

- Задеплоєно на прод 20.08.2026 (коміт — див. git log). **Без міграцій** — схема БД не мінялась (усе в jsonb `extras`).
- **Лишились НЕзакомічені** попередні батчі: Фактури 2.0 / бот-сканер / invoiceArchive+ksefPdf (12.08, з міграцією `2026-08-12-invoice-archive.sql` і `assets/` шрифтами), банк-синк/KLIN (14.08), зміни `lib/db` схеми, `CLAUDE.md`/`PROJECT_MAP.md` (доки мішані по батчах). Їх деплоїти окремо після тесту.

## Після-деплой кроки

1. Прогнати «З обліку годин» на Agram-вкладках відкритого місяця — рядки отримають `extras.facBonus` і перерахуються (залочені місяці не чіпати).
2. Смоук: чіп схованої колонки → показується лише вона; Premia на Agram → у готівці; мінусовий рядок → «Разом» його не мінусує; наступний місяць — борг у тій самій колонці з бейджем ⤵.

## Граблі, на які не наступати

- Тести розкладу фіксують нові правила: konto ≤ доВиплати − премія − бонус (крім студентів до 26). Не «спрощувати» кламп ksiegRatesOf — віднімання facBonus ДО min(…, 25.35) критичне для баз нижче стандартної.
- `extras.facBonus`, `debtIn`, `debtOut` — службові ключі: у веб-колонки не виводити (виняток як `blockOnly`), у сегментному розподілі грошей не ділити.
