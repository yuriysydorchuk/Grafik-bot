# HANDOFF: транспорт/бадання/одяг/національності (сесія 12–13.08.2026)

Разова записка сесії. Довговічні правила/маршрути вже перенесені в `CLAUDE.md` / `PROJECT_MAP.md` — тут стан, знахідки і хвости.

## Що на проді (коміти 0f33847 → cf7f6db, обидва задеплоєні)

- **Зняття за довіз:** вкладка показує ВСІХ людей платних фабрик (віртуальні рядки «не знімається»), бейдж «🚗 сам», імена-лінки; ✎ на віртуальному рядку створює ручне зняття пари (POST з явним `factoryId`) — авторозрахунок його не перетирає. `self_transport_since` («діє з») вирішує режим self помісячно.
- **Залічки (аванси):** `advance_requests.factory_id` — бот питає фабрику при >1, from-hours кладе в її рядок; `paid_by` — хто вручну позначив «виплачено» (сайт і бот-кнопка).
- **Бадання:** `worker_badania` — список записів (не одне поле), CRUD у профілі; вкладка «Бадання до зняття» на /advances (чекбокси, вибір місяця з реальних сводних) → Zaliczka BD (ДОДАЄТЬСЯ до наявного); підвкладка «Зняті» (історія з місяцем) + «↩ Відмінити» (віднімає з клітинки, лок → 409); PATCH-гард від простого зняття позначки перенесеного.
- **Одяг 2.1:** склад `clothing_stock` (тип/розмір/стан/ціна/к-сть), довідник типів `clothing_types` (CRUD з UI), видача зі складу, повернення з вибором стану (нове/БУ, працює і без stockId), «До зняття» → Odzież сводної, «Архів знять», блок «Одяг» у профілі.
- **Національності:** `workers.nationality` (6 регіонів, репрезентативні прапори 🇺🇦🇧🇾🇿🇼🇨🇴🇰🇿🇮🇳) — профіль, довози, сводна.
- Міграції всі накачені на прод (вкл. `deducted_month`); тести 395–404/404 зелені на момент деплоїв.

## Інцидент: бекапи БД мовчки не робились 07–12.08

`pg_dump` падав цілком через суперюзерські таблиці `workers_rate_backup_20260807` тощо (app-юзер не може LOCK). Полагоджено у `deploy/backup.sh` (`--exclude-table='*_backup_*'` + `*fixbak*`, коміт 211c6d1), перевірено запуском як у крона. **Діра 07–11.08 без дампів — незаповнювана.** Якщо захочеться прибрати/переоформити ті таблиці — тільки з-під postgres-суперюзера (вони в списку «не чіпати» з чистки ставок).

## Знахідки по липневих залічках (НЕ закриті, чекають Yuriy)

- 4 липневі «виплачено» (Akpar 500, Skrypnyk 400, Dyminska 500, Pazych 400) позначені **вручну бот-кнопкою «💸 Виплачено»** — не системою і не сайтом (нема paid_txn_id і методу). Хто саме — не записано (тепер пишеться `paid_by`).
- 38 липневих авансів на 18 400 зл досі в статусі «передано до виплати».
- **Можливі між-місячні дублі:** Skrypnyk 400 зл у сводній і за червень (ANDROS KLINEX), і за липень (ANDROS); Pazych 400 — червень (ALMIZ) + липень (ANDROS). Червневі суми — з таблиць/синку, липневі — від системи. Домовленість: **Yuriy чистить вручну**, автоматично не чіпаємо.
- Дублів «одна залічка в кілька фабрик одного місяця» в проді нема (перевірено SQL).

## Паралельний батч фактур — НЕ задеплоєний, живе в робочому дереві

Архів фактур на Drive + KSeF PDF-візуалізація + скан у боті (`invoiceScan.ts`, `invoiceArchive.ts`, `ksefPdf.ts`, `@anthropic-ai/sdk`, міграція `2026-08-12-invoice-archive.sql`, ~25 файлів) — **uncommitted**, веде інша сесія. Мій деплой cf7f6db зроблено ЧЕРЕЗ stash-по-шляхах цього батчу (акуратно відновлений, `git stash pop` чистий). Змішані файли були лише `i18n.tsx` і `PROJECT_MAP.md` — мої ханки закомічені, їхні лишились у дереві. При деплої фактурного батчу не забути його міграцію і, ймовірно, `ANTHROPIC_API_KEY` на проді.

## Хвости / можливі наступні кроки

- Вибір фабрики при перенесенні бадань/одягу (зараз — рядок з найбільшими годинами місяця) — якщо Yuriy попросить.
- Інструкція користувачам у бот про нові фічі (аудиторію підтверджує Yuriy) — не розсилалась.
- `porivnyannya-konto-gotivka-2026-05.xlsx` в корені і 131 scratch-\*.mjs — не закомічені, сміття/робочі файли.
