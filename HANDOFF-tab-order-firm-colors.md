# HANDOFF: порядок вкладок drag-and-drop + кольори фірм (Облік годин / Сводні)

Сесія 03.08.2026. Коміт `1c86a56`, задеплоєно на прод.

## Що зроблено

1. **Персональний порядок вкладок.** Міста і фабрики в «Обліку годин» і «Сводних»
   перетягуються (нативний HTML5 DnD, як у рекрутингу) і зберігаються за
   користувачем у `admins.web_prefs` (jsonb key→value) — їдуть за акаунтом між
   браузерами. API: `POST /auth/web-prefs`, `/auth/me` віддає `prefs`.
   Веб-хелпери — `web/src/lib/prefs.ts` (`useOrderPref` + `orderBy` +
   `useDragOrder`). Ключі: `order.hours.cities`, `order.hours.factories`
   (один список на сторінку — drag працює по повному списку, порядок інших міст
   не губиться), `order.svodni.cities`, `order.svodni.factories.<місто>`.
   Приколоті (не тягнуться): «Всі міста»/«Всі фабрики» (Hours), «Офіс»/«Підсумок»
   (Сводні). Невідомі/нові фабрики стають у кінець за дефолтним сортуванням.

2. **Кольори фірм на кнопках фабрик** (обидві сторінки): ES — червоний,
   ESO — фіолетовий, Klinex — зелений (`FIRM_TAB` у `web/src/lib/colors.ts`,
   буквальні Tailwind-класи). Неактивна кнопка — тонований фон/рамка + крапка,
   активна — червона з крапкою фірми.

## Джерело фірми (важливо)

- Канон — **прив'язка клієнта до фірми в нашій базі**: `factories.company_id`.
  `/hours` віддає `firm` кожному рядку звідти; `GET /svodni` підставляє фірму
  фабрики в рядки, де `svodni_rows.firm` порожня (а вона порожня майже скрізь —
  синк заповнює її лише з заголовків вкладок таблиці та книг Лодзі).
- Виняток — клієнти **з двома фірмами** (ANDROS: Klinex + ES) і офісні вкладки:
  вкладки сводної там уже розділені по фірмах, тож фірма береться з **назви
  вкладки** (`labelFirm` у `Svodni.tsx`: OUTS→ESO перший, бо поруч стоїть ES).
- Дані: 4 фабрикам без фірми проставлено **ES** (EUROCASH KROSNO, RECYKLING,
  MAKARUK, JUKKI — останні дві неактивні, фірма потрібна для історичних
  місяців). Підтверджено власником; UPDATE входить у міграцію.

## Міграція

`deploy/migrations/2026-08-03-admin-web-prefs.sql`:
`admins.web_prefs jsonb NOT NULL DEFAULT '{}'` + UPDATE фірм 4 фабрик.
Накатана на дев і прод.

## Нюанси

- Ключ prefs валідується юнікод-регекспом (`\p{L}`) — міста в ключах кирилицею.
- Порядок пишеться оптимістично в кеш react-query `["me"]`, POST — best-effort.
- У робочому дереві лишається **некомічена «папка водія»** (fleet/transport/
  clothing/fuel/hostels2/analytics + 9 міграцій 31.07–01.08) — свідомо НЕ
  ввійшла в цей деплой (частковий коміт по-ханково: `workers.ts`, `admin-api.ts`,
  `i18n.tsx`, `PROJECT_MAP.md` — змішані файли). Її деплой — після окремого
  сайн-офу власника.
