node_modules
dist
*.tsbuildinfo
.env
.env.*
.DS_Store
*.log

# uploaded files (worker documents, etc.) — persistent, back up separately
uploads/

# but keep the deploy template
!deploy/.env.example

# Хендоф первинного VPS-деплою містить секрети (паролі/токени) — не комітити
docs/archive/HANDOFF-vps-deploy.md

# KSeF certificate/keys — never commit
KSEF/

# Enable Banking (open banking API) RSA key + cert — never commit
secrets/

# Знімки даних для міграцій (реальні дані — не комітити)
data-import/

# Разові scratch-файли (скіл scratch-db, смоуки, аудити) — будь-яке розширення, будь-де; не комітяться
scratch-*
