# HANDOFF: водійські поверхні бачать draft-тижні (27.07.2026)

Сесійна записка. Довговічні правила вже перенесені: CLAUDE.md → «Правила розробки» (вибір тижня лише через `services/weeks.ts`), PROJECT_MAP.md → «Ризикові місця».

## Інцидент

Тиждень 27.07–02.08: графіки створені в пт 24.07, але рядок `schedule_weeks` лишався `draft` до пн 09:55 (затвердили одну фабрику → approved на весь тиждень). Наслідки за вікенд:

- головний водій **не бачив тижня** в боті у «📋 Призначити водіїв» (список був approved-only);
- призначення, зроблені з веб-панелі, лягали на draft-рядок — водії в боті їх **не бачили** («Мій графік», «Моя зміна», посадка/явка);
- у пн зранку **не пішли пресмінні пуші** (крон теж фільтрував approved).

Веб-панель при цьому працювала: вона резолвить тиждень як «approved ?? останній рядок» і навіть створює draft при призначенні наперед. Розсинхронився лише бот+крон.

## Фікс (коміт `04ff845`, на проді з 27.07.2026)

- **`services/weeks.ts` (новий):** `resolveWeekRow(weekStart)` = «approved ?? останній рядок», `ensureWeekRow` = створити draft. Єдина точка правди.
- **Бот:** усі водійські хендлери + `showDriverShift` переведені на `resolveWeekRow`. «📋 Призначити водіїв» завжди пропонує поточний + наступний тиждень (рядок створюється при виборі, дзеркально веб-PUT), решта — наявні тижні з БД, дедуп по `weekStart`, до 8. «Мій графік»/«Графік тижня» показують поточний тиждень (раніше — найсвіжіший затверджений). Гард: вільно введена не-понеділкова дата не створює сміттєвий рядок.
- **Крон:** пресмінні пуші, pickup-нагадування, прогалини забору — через `resolveWeekRow`.
- **admin-api/notify:** переведені на хелпер без зміни поведінки.
- **Не чіпали:** робітничі екрани бота, `/hours`, reliability, сводні — approved-only за дизайном; офісний флоу «Розіслати затверджений графік» — теж.

## Тести

`bot/driver-draft-week.integration.test.ts` — 5 кейсів (без фікса падають). Побічно полагоджено застарілий `fac`-тест самореєстрації в `deeplink.integration.test.ts` (після 18e89aa мова обирається першою) — він був червоний на main ще до цієї сесії.

## Стан на кінець сесії

- Прод: healthz ok, логи чисті. Дані НЕ правились (тиждень 27.07 вже approved — затвердили вручну вранці до фікса).
- TODO (не зроблено): повідомлення головному водієві про те, що поточний/наступний тиждень тепер завжди доступні для призначення — Yuriy підтверджує аудиторію і текст.
