# HANDOFF: P&L «По містах» — Scandic Food + протікання cleaning-доходу

Сесія 25–26.08.2026. Розбір питання власника: «чому Скандік Фуд не мапиться в П&Л на Люблін» + «WSPÓLNOTA ZYGMUNTA AUGUSTA 29, 495 zł — це взагалі прибирання».

## Що зʼясовано

1. **Дохід у «По містах» не дивиться на `factories.city`.** Він ділиться по містах ∝ собівартості клієнта з `payroll_factory_months`, а зшивка revenue↔cogs іде по **точному текстовому label** (регістрозалежно). У фабрики SCANDIC FOOD (прод id=25) були порожні `client_nip`/`pnl_label` → cogs-рядок звався «Scandic Food» (з вкладки сводної), revenue — «SCANDIC FOOD» (з назви покупця KSeF) → дохід 10 080 zł падав у «нерозподілене».
2. **Вспульнота 495 zł була класифікована правильно** (`segment='cleaning'`, фактура C1/8/2026). Але `GET /pnl/cities` читав P&L-рядки **без фільтра сегмента** — на відміну від «По клієнтах» — і cleaning-дохід протікав у «нерозподілене» вигляду «По містах» (у 2026-05/06 так висіли всі вспульноти).

## Що зроблено

- **Прод-дані (з дозволу власника, 26.08):** `factories id=25` → `client_nip='5261158435'`, `pnl_label='Scandic Food'`; relabel `ksef_invoices id=3626` (A3/8/2026) і липневого revenue-рядка `pnl_entries` на «Scandic Food» (еквівалент `POST /ksef/rematch` точково). Бекапи «до» — `/root/backups/scandic-{factories,ksef,pnl}-2026-08-26.csv`. Наступні KSeF-синки матчать по NIP самі.
- **Код:** `routes/pnl.ts` `GET /pnl/cities` — рядки P&L фільтруються `(segment ?? 'main') === 'main'` (один фільтр на revenue і fixed). Typecheck/тести зелені.

## Верифікація

- Липень «По містах»: Scandic Food — дохід 10 080 zл на Любліні поруч із cogs 7 115,77.
- «Нерозподілене» за 2026-05/06/07 більше не містить WSPOLNOTA-рядків (у /cleaning вони як були, так і є).

## Хвости

- Немає. Довговічний факт (main-only фільтр у /pnl/cities) внесено в PROJECT_MAP.md тим самим комітом.
