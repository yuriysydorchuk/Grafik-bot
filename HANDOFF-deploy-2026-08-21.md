# HANDOFF: деплой 21.08.2026 — фікс Telegram ID + Фактури 2.0 + банк-синк + гейти

Сесія 19–21.08.2026. Задеплоєно на прод чотири батчі (після кара-батчу `11e7293`, який пішов окремою сесією).

## Що поїхало (коміти)

| Коміт | Що |
|-------|-----|
| `f6552ff` | **fix(workers): дубль Telegram ID** — POST/PATCH `/workers` перевіряють дубль `telegramId` → людське 400 з іменем власника привʼязки (за зразком workerCode) замість 500-ки з БД. +3 інтеграційні тести |
| `2bfee17` | **Фактури 2.0** — Drive-архів фактур, PDF-візуалізація KSeF (`ksefPdf.ts`, шрифти DejaVu, QR), спосіб оплати/термін/overdue на /cost-invoices, капа `invoiceScan` для бот-сканера. Схема: `invoices`/`ksef_invoices` + drive_*/payment_*/due_date (міграція `2026-08-12-invoice-archive.sql`). Нові деп-си `@pdf-lib/fontkit`, `qrcode` |
| `3decf67` | **Банк-синк** — матч рахунків по цифровому ядру NRB (MT940 `/PL…` vs голий IBAN з API), KLINEX→KLIN (одрук «Klinekx» ховав витяги), unmatched-звіт про папки/loose `.mt940` поза синком + тост на /bank |
| `7de0b01` | **Скоупінг гейтів** — усі глобальні `router.use(requireCap(...))` скоуплено по префіксу шляху (латентний баг: роль без viewFinance не діставалась до /cash, /cost-invoices, /fuel) |

## Інцидент, з якого все почалось (19.08)

`PATCH /api/workers/363` (Gadza Louis, дубль-профіль) → 500 `workers_telegram_id_key`:
адмін вписав Telegram ID, привʼязаний до іншого профілю, отримав незрозумілу помилку
і просто **звільнив профіль**. Тепер це 400 з підказкою. NB: для дублів профілів
правильний інструмент — `services/workerMerge.ts` (переносить telegram + записи),
а не «звільнити дубль»; актуально для чистки дублів Skrypnyk/Pazych.

## Деплой-специфіка

- Міграція `2026-08-12-invoice-archive.sql` накачана на прод (ідемпотентна, `IF NOT EXISTS`).
- `build.sh` підтягнув нові npm-залежності; шрифти `artifacts/api-server/assets/fonts/` приїхали з git.
- Верифікація перед пушем: typecheck, повний тест-прогін (424/424, локальна `grafik_bot_test`), збірка api+web.
- `mockup-sandbox` не збирається без `PORT` (vite.config вимагає) — пісочниця, не прод; `pnpm run build` цілком через це червоний, збирати цільово web+api.

## Разові кроки після деплою (Yuriy)

- [ ] Видати капу `invoiceScan` ролям, які мають сканувати фактури з бота (Settings → Ролі).
- [ ] Натиснути «Синк KSeF» на /cost-invoices і глянути, що архів на Drive наповнюється (колонка Drive, червоні `drive_error`).
- [ ] Минулі місяці на Drive доганяються кнопками «залити місяць» (крон тягне лише поточний).
- [ ] За бажанням — коротка інструкція користувачам у бот про /cost-invoices (аудиторію підтверджує Yuriy).

## Відкриті хвости

- Лютий–квітень 2025 loose `.mt940` — **свідомо** не імпортовані (рішення 14.08), тепер видимі в unmatched-тості.
- Перепривʼязка Telegram ID «в один клік» (перенесення з профілю на профіль) — не робилась; зараз конфлікт чемно блокується, перенос — через workerMerge або вручну.
