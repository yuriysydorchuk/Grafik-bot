# HANDOFF: сводна — стейл-ставки 31.5, нульові сегменти, веб-фікси (13.08.2026)

Записка сесії. Довговічні висновки вже перенесені: PROJECT_MAP.md (блок «Сводні» → «Веб-вкладка — порядок і кеш») + памʼять `rates-cleanup-2026-08`.

## Що було (симптоми Yuriy на проді)

1. Ставка 31.5 у липневій сводній (Ishchenko Maiia та ін.), хоча на фабриці прописано 31.4.
2. «Дублі»: у людини 2 рядки, один із 0 годин.
3. Після будь-якої ручної правки клітинки рядок стрибав униз у групу «Без фірми» (до F5).
4. Не всі вкладки відсортовані по алфавіту.

## Корені (все підтверджено даними проду)

1. **31.5 = снапшот профільних ставок до чистки 07.08.** From-hours 04.08 створив рядки, коли в профілях 13 людей ще стояла `hourly_rate=31.5` (доказ — прод-таблиця `workers_rate_backup_20260807`); чистка обнулила профілі, але from-hours свідомо не оновлює ставки наявних рядків (`prev.rateNetto`, routes/svodni.ts). 36 рядків ANDROS + 3 PREMIUM FRUITS.
2. **«Дублі» — сегменти (🧩), не дублі.** 05.08 масове донесення birthDate/legalStatus (admin_id=7) з «діє з» всередині липня порізало рядки; люди почали працювати ПІСЛЯ дати → вікно «до» = 0 год (14 з 28 сегментів липня). `planSegments` не має гарда на порожні вікна.
3. **Стрибок у «Без фірми»**: GET /svodni збагачує `firm` з фабрики, а PATCH-відповідь — ні; веб замінює рядок у кеші відповіддю цілком → рядок без фірми серед збагачених.
4. **Алфавіт**: дефолтне сортування вмикалось лише при секціях/мультифірмі; вкладки без посад лишались у порядку створення рядків.

## Що зроблено

- **Дані (руками Yuriy через UI, перевірено скриптами):** 13 рядків видалено і перестворено from-hours (джерело «Години з фабрики») → всі 31.4, без сегментів, 31.5 у липні не лишилось. Ishchenko 80×31.4=2512 ✓. Mahura: год. повідомлення 40 підтяглись ✓. Songe (PREMIUM FRUITS): Dojazd 117 на місці ✓. Сиріт-сегментів нема (FK cascade). Балабей — легальна порізка, не чіпали.
- **Код (деплой `ea40eeb`, 13.08):** `enrichFirms` у всіх 3 гілках PATCH-відповіді; дефолтний порядок фірма→секція→імʼя на всіх вкладках, КРІМ синкованих з Google (`source_id`; наразі лише Лодзь — рішення Yuriy «синкові не чіпаємо»); `serializeRow` віддає `sourceId`. Смоук: healthz ok/ok/up.

## Не зроблено / далі

- **Гард на порожні сегменти** у `planSegments` (не створювати 0-годинне вікно при донесенні порожнього поля) + підказка в модалці «Діє з» («дата пізніша за перші години місяця → порізка з 0-год сегментом») — запропоновано, Yuriy ще НЕ підтверджував.
- **Червень з 31.5** (ANDROS KLINEX, Sushi&Food, SUPERDROB, ALMIZ — 60 рядків) — історичні, ймовірно легітимна тодішня ставка; НЕ чіпати без окремого підтвердження.
- Прод-таблиці `workers_rate_backup_20260807` / `svodni_rows_backup_20260807` (суперюзерські, видні лише під postgres) — прибрати після затвердження липневої сводної.
- Mashoko Media: студентка, але вже НЕ «до 26» (по даті народження) → оподаткована 25.35 — Yuriy попереджений, це коректно.
- PROJECT_MAP.md правлений, але НЕ закомічений — файл брудний змінами паралельного батча «Фактури 2.0» (12.08), поїде з тим комітом.
