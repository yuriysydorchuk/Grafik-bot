# HANDOFF: Eurocash — авторозрахунок сводної з файлів фабрики (Lublin + Białystok)

Сесії 06.08 і 10.08.2026. Канонічна документація — `PROJECT_MAP.md` (розділи
«Облік/відсутності» і «Сводні»); тут — контекст, рішення власника і що лишилось.

## Оновлення 10.08.2026 — Białystok

- **Формат ROZLICZENIE** (`ROZLICZENIE EURO SUPPORT ZA 07.2026.xlsx`): рахуємо
  ЛИШЕ з колонок Suma czasu pracy / Godziny nocne / Stawka netto / Potrącenia
  za błędy / Inne potrącenia (рішення власника; Wydajność/Próg/Dodatek не
  читаємо). «Stawka netto» файлу = ставка фабрики агенції → матч із зеленим
  рядком блоку STAWKA EUROCASH → поріг → пара нетто/брутто працівника. Це
  покриває і OPGD/wózkowych (їхній поріг НЕ виводиться з жовтих діапазонів —
  Shchepetilnikov: продуктивність 87–96, поріг 3 по ставці 44,92). Потроненя
  za błędy + inne — сумою в potrącenia. Login → ключ фабрики (матчинг «🔑
  Ключі»), місяць — з колонки Miesiąc. `EUROCASH_FACTORY_IDS` = {9, 8}.
- **Дзеркало блоку** — до порожнього рядка (макс 8): норми wózkowych Білостока
  («operacji GD na godzinę» / «kartonów na operację GD») тепер у комплекті.
- **Інфо-таблиця щомісяця**: `GET /svodni` підкладає вкладці без блоку
  найсвіжіший минулий блок цього ж міста+label (ставки міняються ~раз на рік,
  вкладку нового місяця в Google більше не ведуть — сводну робить сайт).
- Помилка 06-08.08 (мінімалка 25,35 трьом новим у Любліні): корінь — на проді
  не було дзеркала блоку (синк Любліна не ганявся відколи існує дзеркалення
  info). Виправлено: блок засіджено SQL-ом (повна точність з живої таблиці),
  код тепер ПРОПУСКАЄ Eurocash-пару без вирішеного порога (тихий фолбек на
  стандартну пару — заборонений, `eurocashUnmatched` пояснює причину).
- Разом із цим деплоєм запушено весь локальний батч (водійська папка:
  автопарк 2.0 / транспорт / хостели 2.0 / одяг / пальне / аналітика +
  дрібніше) — сайн-оф власника 10.08; міграції 2026-07-31…2026-08-03 накачені
  на прод у цьому ж деплої.

## Сесія 06.08.2026 — Lublin

Задеплоєно (коміт `df76907`; міграція `2026-08-06-factory-hours-extras.sql`).

## Що зроблено

Ланцюг: **файл фабрики → Облік годин → сводна**, все автоматично:

1. **Парсер** (`services/factoryHours.ts`, формат `eurocash`, під тестами):
   розрахунковий файл Eurocash (`EUROSUPPORT LIPIEC 2026.xlsx`; заголовки
   `Nazwisko i Imię … Suma z Wyk. [h] … Stawka H NEW AGENCJA`). Крім годин
   тягне extras: нічні години, продуктивність punktowa, ставку агенції,
   потроненя, korekta/końcowe, NR OSOBOWY → `factory_hours.extras` (jsonb).
2. **from-hours** (`routes/svodni.ts`): для фабрик з `EUROCASH_FACTORY_IDS`
   (= {9} EUROCASH LUBLIN, `services/svodni.ts`) пара ставок нетто/брутто
   береться від **порогу продуктивності**: блок STAWKA EUROCASH вкладки сводної
   (дзеркало `svodni_tab_meta.info.stawkaEurocash`; найсвіжіший місяць ≤
   поточного). Поріг матчиться по ставці агенції з файлу (±0.011), фолбек —
   продуктивність у діапазони жовтого рядка (`eurocashRatesFromBlock` /
   `eurocashBracketIndex`, під тестами). Студент до 26 — нетто = брутто і нічна
   4,50; інші — нетто-рядок і нічна 3,50 (значення — клітинки A блоку, не
   хардкод). Нічні → `extras.nocneH × doplataNocna` у формулі виплати,
   потроненя фабрики → колонка potrąceń. Кому ставка не порахувалась —
   `eurocashUnmatched` у відповіді (з причиною), веб показує тост.
3. **Веб** (`Hours.tsx`): превʼю імпорту показує нічні/продуктивність/ставку/
   потроненя; після «Застосувати» Eurocash-файла **авто-тригериться перенос у
   сводну** (from-hours, source=factory, ця фабрика). У тулбарі новий селект
   **«Імпорт файла фабрики…»** — точка входу для першого імпорту місяця, коли
   вкладки фабрики ще нема (у Eurocash графік у системі не ведеться, тож без
   цього кнопку імпорту не було де натиснути).
4. Дзеркало блоку STAWKA EUROCASH тепер зберігає числа **повної точності**
   (раніше округлялось до копійки) — ставки рахуються як формули таблиці.
   Стане чинним для липня після першого синку сводних з липневою вкладкою.

## Рішення власника (сесія 06.08)

- **Księgowość — фактичні години** (не «більше годин по мін. ставці», як
  виглядало у червневій таблиці: 188,25 = виплата ÷ 30,50). Стандартна логіка
  applyLegalDefaults.
- **Виплата вся на конто** і студентам, і ні (payoutPref all_konto на рівні
  рядка; явний payoutPref з профілю має пріоритет).
- **KOREKTA / KOŃCOWE ROZLICZENIE** — агентський рівень (розрахунок з
  фабрикою), у ЗП працівника не йдуть; зберігаються в extras довідково.
- **Авто-перенос** у сводну одразу після застосування годин — підтверджено.

## Звірка (валідація формули)

Червень 2026, NKOMO THABO (студент): 158 год × 35,675936837… (поріг 6 по
ставці агенції 48,83) + 65,5 нічних × 4,50 − 190 кари = **5 741,55** — збіг із
клітинкою таблиці до копійки. Липневий файл: 5 людей, пороги 7/7/2/1/2
(смоук `artifacts/api-server/scratch-eurocash-smoke.mjs`).

## Що НЕ покрито / далі

- **EUROCASH BIAŁYSTOK (id=8) і KROSNO (id=17)** — інші таблиці порогів
  (у Białystok свій блок з рядком «Wózkowi») і, можливо, інша система —
  лишаються вручну. Додавання: розширити `EUROCASH_FACTORY_IDS` після звірки
  їхніх правил з власником.
- Липневий блок порогів підтягнеться після синку сводних (зараз from-hours
  бере червневий — власник каже, ставки міняються рідко).
- Троє нових людей з липневого файлу (Basanskyi, Ndlovu Fortune, Shlapa) —
  профілі створюються з превʼю імпорту («➕ Створити профіль», вибрати фірму);
  статус студент/дата народження впливають на ставку — заповнити в профілі.
- `factories.night_addon` (3.5/4.5 по фабриках 8/9/17) — легасі-поле, у
  Eurocash-розрахунку НЕ використовується (нічні йдуть з блоку порогів).

## Обережно: незакомічений фінбатч

У робочому дереві досі лежить залишок фінансового батчу (див. памʼять
`uncommitted-finance-batch`). Цей деплой зібраний **вибірково** через
тимчасовий індекс: змішані файли (`workers.ts` схема, `admin-api.ts`,
web `i18n.tsx`, `PROJECT_MAP.md`) закомічені як «HEAD + лише Eurocash-хунки».
Не робити `git add -A`.
