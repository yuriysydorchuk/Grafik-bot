-- Фірма «укладає умови з працівниками» (05.09.2026). false — приватні підприємці
-- власників (RS/TS): є у фінблоці, але не роботодавці → не показуються у списках
-- «фабрика · фірма» (чипи фабрик, модалка генерації умов). Прапорець ставиться
-- в Налаштування → Фірми; id фірм локально/прод різняться — тут НЕ сідимо.
ALTER TABLE companies ADD COLUMN IF NOT EXISTS employs_workers boolean NOT NULL DEFAULT true;
