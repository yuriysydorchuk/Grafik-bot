-- Причина відхилення запиту на вихідний/відсутність: опційний текст,
-- який офіс вписує при відхиленні (веб або офіс-бот) — додається у
-- сповіщення працівнику.
ALTER TABLE absence_requests ADD COLUMN IF NOT EXISTS reject_reason text;
