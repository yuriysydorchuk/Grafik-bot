-- Походження профілю працівника: "hours_import" = створений опцією
-- «створити профіль» в імпорті годин фабрики (/hours). Лише такі профілі
-- можна швидко видалити 🗑 просто зі списку обліку годин (випадковий клік).
ALTER TABLE workers ADD COLUMN IF NOT EXISTS created_source text;
