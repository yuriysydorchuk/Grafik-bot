-- Дата працевлаштування у профілі працівника (усі працівники).
-- Для фабрик Agram від неї автоматично рахується стаж-бонус до ставки нетто
-- (+1 зл/год після 1 місяця, +1.5 після 6 місяців).
ALTER TABLE workers ADD COLUMN IF NOT EXISTS employment_start_date date;
