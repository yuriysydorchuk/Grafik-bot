# HANDOFF — деплой 26.08.2026: Фактури 2.1 (архів v2, історія змін, злиття сторінок)

Одноразова записка деплою. Довговічні факти вже перенесені в `CLAUDE.md` / `PROJECT_MAP.md`.

## Що поїхало

1. **Архів на Drive v2:** імʼя файла = «номер фактури + контрагент» (закупівлі/скани — постачальник, продажі — покупець), на Диску **лише PDF** (KSeF-XML більше не заливається; лишається локально в `uploads/ksef-xml/`). Разовий `upgradeArchiveNamesV2()` (guard-настройка `invoice_archive_names_v2` у settings) сам перейменує вже залиті PDF і прибере XML у кошик Диска — спрацює при першому кроні 06:00 або кнопці «Синк KSeF».
2. **Ручна фактура на сайті не зберігається без фото/скана** (файл їде в архів за тими самими правилами).
3. **Індикатор архіву:** «☁️ Всі фактури збережено на Google Диск (N)» / «На Диску N з M» на обох розділах.
4. **Журнал змін фактур** (`invoice_audit` + `services/invoiceAudit.ts`): хто додав/змінив/затвердив оплату, diff полів «було → стало»; модалка «Історія фактури» (іконка 🕘). Пишеться з сайту і бот-сканера; **історія веде відлік з цього деплою**.
5. **Злиття сторінок:** /ksef і «Фактури (таблиця)» видалені з навігації (роути — редіректи). «Фактури» = розділи «Фактури закупові» + «Фактури спшедажові». Спшедажові відкриті і бухгалтерії (гейт /ksef → `requireAnyCap(viewFinance, costInvoices)`). Wspólnoty (segment=cleaning) зі списку спшедажових сховані — вони в розділі «Прибирання»; в Drive-архів заливаються.

## Міграція

- `deploy/migrations/2026-08-26-invoice-audit.sql` — таблиця `invoice_audit` + індекс. Накатана на прод у цьому деплої (див. звіт у чаті).

## Разові кроки після деплою (Yuriy)

- [ ] Натиснути «Синк KSeF» на /cost-invoices (або дочекатися крону 06:00) — разовий апгрейд перейменує вже залиті PDF у «номер + контрагент» і прибере XML з Диска.
- [ ] Кнопками «Місяць на Drive (N)» догнати минулі місяці (авто-архів тягне лише поточний місяць).
- [ ] Якщо капа «Сканування фактур у боті» (invoiceScan) ще не ввімкнена ролі кшєнгової — Налаштування → Ролі.
- [ ] Коротка інструкція кшєнговій у бот (зміни UI: розділи, історія, обовʼязковий скан) — аудиторію підтверджує Yuriy.

## Верифікація перед деплоєм

- typecheck усіх пакетів; **повний тест-прогін 447/447** (юніти + інтеграційні на одноразовій БД), обидві збірки.
- Живі смоуки на реальних KSeF/Drive: перейменування PDF (`FS 2017_MAG_08 A.M.S. Centrum Sławomir Łasiński.pdf`), прибирання XML (24 рядки), QR-хеші звірені з еталонними invoice_hash KSeF (6/6 MATCH).

## Примітки

- 26.08 знайдено й прибрано детачнутий локальний `node dist/index.mjs` (тримав :8080 повз pm2) — через нього частина локальних тестів Yuriy 25–26.08 ходила по старій збірці; зміни того періоду в журнал не потрапили (функції ще не існувало).
- Прод-архів, залитий з 21.08 зі старими іменами, апгрейд перейменує сам; файли, залиті локальними тестами (дублікати з іншими file id), можна вручну прибрати з Диска в кошик.
