# HANDOFF: закріплені шапки (Працівники, Облік годин) + вкладки міст/фабрик (23.07.2026)

Разова записка сесії. Лише веб-панель — без змін API, схеми БД чи i18n-логіки.

## Що зроблено

- **`web/src/pages/Workers.tsx`** — панель пошуку/фільтрів (пошук, фабрика/фірма/посада,
  «Звільнені», кнопка посилань) закріплена під верхнім баром при скролі (md+ only).
  Патерн 1-в-1 зі Schedule.tsx: `md:sticky md:top-[52px] md:z-20 md:-mx-8 md:bg-[#f6f7f9] …`.
- **`web/src/pages/Hours.tsx`**:
  - уся верхня панель (місяць, бейджі підсумків, «до сводної / нагадати / Excel»)
    у тому ж sticky-контейнері;
  - у шапці два ряди перемикачів: сегментований перемикач **міст** і кнопки-вкладки
    **фабрик** вибраного міста з лічильником людей (стиль вкладок Сводних).
    Кнопки **фільтрують** секції нижче; вибір фабрики скидається при зміні міста;
    невалідний вибір після перемикання місяця тихо падає на «всі»;
  - порядок міст: реальні міста за алфавітом → **«Без міста»** → кнопка **«Всі міста»**
    остання (те саме сортування застосовується і до секцій списку);
  - бейджі підсумків зверху лишаються загальними по місяцю (не фільтруються).
- **`web/src/lib/i18n.tsx`** — новий ключ `"Всі міста": "All cities"`.

## Перевірено

- `pnpm --filter @workspace/web run typecheck` — чисто; `run build` — ок.
- У браузері власник ще не дивився.

## Не зроблено / хвости

- **Не закомічено й не задеплоєно** — чекає перевірки власником (правило: деплой
  лише після явного підтвердження).
- У робочій копії паралельно висить великий незакомічений пласт іншої роботи
  (svodni2/payroll/ksef тощо, див. `git status`) — ці три файли комітити окремо
  від нього.
