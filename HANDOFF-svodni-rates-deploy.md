# HANDOFF: сводні v1 — правила ставок, вік «до 26», мердж даних на прод (24.07.2026)

Записка сесії 22–24.07.2026. Канонічні деталі вже перенесені в `PROJECT_MAP.md`
(секція «Сводні») і `CLAUDE.md`; тут — що саме сталося, стан прода і хвости.

## Що задеплоєно (коміти `170b2c1`, `552e867`, `737be94`)

1. **Резолюція ставок**: профільна ставка = override (`workers.hourly_rate` nullable,
   NULL = «авто»); авто-ланцюг: пара посади фабрики → найдешевша посада («звичайний
   працівник», кейс ANDROS без посади) → базова пара фабрики → стандартна.
   Студент до 26: нетто = брутто, профільна оподаткована netto ігнорується.
   EUROCASH — свідомо вручну (ставка залежить від норм). `resolveBaseRates` під тестами.
2. **Вік «до 26»** — на момент розрахунку (сьогодні), лок сводної фіксує вік датою
   затвердження (`freezeUnder26AtLock`); без дати народження і прапорця — людина
   рахується старшою (правило власника). Кейс-тригер: Nyoni (26 виповнилось 08.07 —
   таблиця DEZYNFEKCJA була права зі ставкою 25.35).
3. **LST (id=1) — бонусна фабрика**: нал +1 зл/год (та сама галочка `agram_cash_bonus`),
   стажевого нема; `CASH_BONUS_FACTORY_IDS`/`factoryBonusPerHour`. Зворотні синки
   ставок віднімають бонус лише не-студентам.
4. **Сводна 2.0 видалена повністю** (код+сторінка+таблиці, міграція
   `2026-07-24-drop-svodni2.sql`). `/penalties` врятовані в окремий
   `routes/penalties.ts` (DDL відновлено з бандла — файли 2.0 ніколи не комітились).
5. **Security**: ехо-відповіді create/PATCH/fire/restore працівника і POST/PATCH
   фабрики фільтруються гейтами GET; `positions` у `/factories` без viewFinance
   зберігають старі ставки (діра «тихого стирання» закрита); бонусні галочки і дата
   працевлаштування — редагування лише `svodniSensitive` (PATCH + profile-apply +
   журнал + видимість). Аванси під `editData` — свідомо ок.
6. **`uses_scheduling`**: 10 фабрик (Лодзь ×6, Sushi&Food, EUROCASH ×3) вимкнені з
   замовлень/графіків/генерації/доступності; галочка в налаштуваннях фабрики.

## Мердж даних локаль → прод (24.07, виконано)

Прод був порожній по сводних (2.0 і вся звірка жили локально). Перенесено
матчингом по імені: 10 фабрик створено, 241 профіль створено (без Telegram),
202 профілі оновлено зарплатними полями, червнева сводна 394 рядки, 162
хостел-зняття, 99 штрафів, журнал змін, 5 фактур KSeF перемаплено по NIP.
Контроль: тотали «до виплати» по фабриках прод = локаль до копійки.

**Бекап прод-БД до мерджу**: `~/backups/grafik_bot_pre-merge_20260724_150342.dump`
(на сервері) + копія в `~/backups/` на маку власника. Відкат: `pg_restore --clean`.

Операційні дані прода (telegram-привʼязки, явки, графіки, аванси) мердж не чіпав.

## Звірка червня (сайт = джерело правди)

Лодзь і Люблін зійшлися з гугл-таблицями до копійки, крім задокументованих
хвостів таблиць (позначені ⚠ у рядках): ANDROS ES — 7 помилок формули (кара не
віднімається), ALLMIZ — 3 рядки без ставки нетто + хостел Samushonga 85 +
Konarbayeva/Yerzhanova (по 252, у таблиці рядків нема), SUPERDROB — вкладка
не заповнена. Познань у таблиці не дороблена (не звірялась).

## Хвости / відкриті питання

- **Травневої сводної немає ніде** (жила у видаленій 2.0) — за потреби відбудувати
  from-hours + разові переноси.
- **Hnatiuk Bohdan**: дата народження в базі `2023-08-15` — битий рік, власник поправить.
- **9 InPost-студентів** з netto=brutto без дат народження — лишені як профільні
  override; якщо власник впише дати, можна перевести на «авто».
- Свідомі профільні override (не чіпати): Nuraliev/Nazirov 31.4/31.4 («на руки»),
  Levchuk 32.9, Miah 31.5/30, Hresko, Kholodniak/Kovalchuk 33/33, Lenartowicz
  32.5/25.95 (так у таблиці ANDROS KLINEX), Duver, Eurocash blended.
- **Сповіщення користувачам** про нові фічі в бот — не надіслане (аудиторію
  підтверджує власник).
- Прод-скрипти сесії: `scratch-merge-prod.mjs` (мердж, більше не запускати без
  потреби — не ідемпотентний по svodni_rows: чистить і заливає наново),
  `scratch-cmp-june.mjs` (звірка з таблицями), `scratch-sec-audit.mjs` (смоук
  доступів) — локальні, не закомічені.
