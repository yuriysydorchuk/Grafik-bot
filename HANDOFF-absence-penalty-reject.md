# HANDOFF: штраф 200 зл + причина відхилення вихідного + липкі тижні доступності

Сесія 31.07.2026. Задеплоєно на прод комітом `50918d8` (main), pm2 рестартнуто, healthz ok.

## Що зроблено

1. **Штраф за пропуск: 300 → 200 зл.** Єдина константа `DEFAULT_ABSENCE_PENALTY`
   (`routes/admin-api.ts`). Діє й ретроактивно — скрізь, де штраф не правили вручну
   (`absence_penalty IS NULL`); override-и (0 / своя сума) не зачеплені. Підтверджено
   власником: ретроактивність ок.
2. **«Доступність»: кнопки тижнів закріплені** (`web/pages/Availability.tsx`) — sticky-бар
   під топбаром за зразком «Графіків» (Schedule.tsx). Каскад sticky-офсетів: бар тижнів
   52px → шапка фабрики 144px → рядок днів таблиці 184px (md+; на мобільному sticky нема).
   Якщо верстка бару стане вищою (перенос кнопок) — офсети треба підкрутити.
3. **Причина при відхиленні вихідного/відсутності** (опційна):
   - БД: `absence_requests.reject_reason` (міграція `deploy/migrations/2026-07-31-absence-reject-reason.sql` — **накатана на прод і локально**).
   - Веб (`Absences.tsx`): «Відхилити» → інлайн-поле причини в картці запиту.
   - API: `POST /absence-requests/:id/reject {reason?}`.
   - Офіс-бот (`bot/index.ts`): після ❌ — стан `absence:reject_reason`, текст або `/skip`
     (той самий патерн, що відхилення авансу); оригінальне сповіщення штампується підсумком.
   - Сповіщення працівнику: рядок `notif.absRejectReason` у `bot/i18n.ts` — 5 мов, текст через `mdSafe`.

## Прод

- Деплой за скілом `/deploy`: pull → міграція → build.sh → рестарт. Смоук:
  `{"status":"ok","db":"ok","bot":"up"}`, у логах лише штатний startup-алерт.
- Обидві графікові (admins role=scheduler: Viktoriia, Grafikowa 2) отримали
  інструкцію в бот 31.07.2026.

## ⚠️ Для наступної сесії

У локальному робочому дереві лишилися **незакомічені зміни іншої задачі**
(fuel/hostels/pnl/invoices/roles: `admin-api.ts`, `workers.ts`, `PROJECT_MAP.md`,
web-сторінки тощо) — цей деплой їх свідомо **не** включав (вибірковий hunk-level
коміт). Вони чекають на завершення/деплой своєї задачі — не губити і не змішувати.
