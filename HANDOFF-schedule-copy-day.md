# HANDOFF: «На тиждень» у графіку + фікс drag до генерації (06.08.2026)

Сесійна записка. Довговічне вже перенесено: маршрути — у `PROJECT_MAP.md` (блок «Графік»).

## Що поїхало на прод

Гілка `feature/schedule-copy-day` → `main` (fast-forward), деплой 06.08.2026:

1. **feat: «📋 На тиждень»** (`8a03f1b`) — кнопка біля заповненого дня у веб-графіку: розставити людей цього дня в ті самі зміни решти тижня. Новий `POST /schedule/copy-day` (`editData`): `preview` — класифікація по кожному цільовому дню (ок за диспозиційністю / без диспозиційності, з приміткою про іншу зміну / відпросився з причиною / пропуск: busy або скасована клітинка); `apply` + `force`-пари — вставка, копія разового часу зміни джерельної клітинки на цільову дату, `hoursOverride` як у `POST /schedule/entry`. Модалка: чипи днів (типово — дні після джерельного), поіменні чекбокси попереджених, масові «Поставити всіх» / «Лише з диспозиційністю». i18n uk/en.
2. **fix: drag до першої генерації** (`edd42d9`) — `POST /schedule/entry` створював 404 «Тиждень не знайдено», поки тиждень не згенерований; тепер сам створює draft через `ensureWeekRow` + валідація `weekStart`.
3. **Тести** (`7dbf2dd` + fix-коміт): 7 нових інтеграційних (`copy-day.integration.test.ts`, доповнений `schedule-extras.integration.test.ts`); у тест-харнес доданий `shiftCancellationsTable`. Повна свита 342/342.

Схема БД не змінювалась, міграцій немає. Графікових сповіщено в бот після деплою.

## Відомі хвости

- **Баг «Додати людину» показує зайнятих в інших змінах дня** — свідомо НЕ фікшений (рішення Yuriy 06.08: «2 не чіпаємо»). Сервер такі кліки й так відбиває 400.
- **Локальний `main` позаду `origin/main`**: прод оновлювався пушем гілки (`git push origin feature/schedule-copy-day:main`), бо в локальному робочому дереві висить незакомічений фінбатч у тих самих файлах (`admin-api.ts`, `i18n.tsx`, `PROJECT_MAP.md`). Звичайний `git pull` у брудному дереві відмовить — підтягувати `main` через рецепт tmp-index (див. память `uncommitted-finance-batch`) або після коміту фінбатчу.
- Робочий worktree сесії (`scratchpad/copy-day-wt`) — тимчасовий, можна зносити: `git worktree prune` після видалення каталогу.

## Флейкі-урок тестів

Якщо інтеграційні тести валяться пачками з `duplicate key "expense_categories_key_key"` у `resetDb` — це ПАРАЛЕЛЬНИЙ тест-прогін на тій самій `grafik_bot_test` (осиротілий раннер від обірваного пайпа `pnpm test | head`). Перевір `ps aux | grep test-hooks`, добий зайвих; не обрізай вивід тест-прогону пайпом.

## 11.08.2026: точна копія + очистка + ІНЦИДЕНТ b94e5fe

Гілка `feature/schedule-copy-tweaks` (за запитом Yuriy):

1. **Точна копія.** «На тиждень» більше не питає по людях: копіюються ВСІ люди дня
   (диспозиційність/відпрошення — інформаційний банер, не фільтр; веб шле `force`
   на всіх). Жорсткі пропуски лишились: busy того дня і скасовані клітинки.
   API copy-day не мінявся.
2. **Перемикач обсягу:** «До кінця тижня» (default) / «Вибрані дні» (чипи).
3. **`POST /schedule/clear`** + кнопка «Очистити» у верхньому рядку дій: видаляє
   записи фабрики тижня для змін, що ще не почались (Варшава, override-aware;
   зміна без відомого часу = майбутня). Минулі зміни і призначення водіїв не чіпає.

**ІНЦИДЕНТ (виявлено 11.08):** коміт `b94e5fe` («весь локальний батч водійської
папки», 10.08) вніс у `admin-api.ts` СТАРУ копію файлу і затер чужі зміни:
`POST /schedule/copy-day` (кнопка на проді била в 404), `POST /advances` (офісна
подача → 403/404), `PATCH /advances/:id`, `POST /worker-bank-accounts/:id/primary`,
групи виплат 15/30 у `decideAdvance`, IBAN-збагачення `GET /advances`,
`ensureWeekRow`-фікс у `POST /schedule/entry`, а також док-рядок у PROJECT_MAP.
CI (інтеграційний джоб) був червоний з 10.08 — це і був сигнал. Усе відновлено
з `b94e5fe^` у цій гілці (тести 367/367). **Урок:** батч-коміт, що «переносить
файл цілком» з іншого дерева, мусить спершу ребейзнутись на свіжий main і пройти
CI; червоний integration-джоб після мержа = СТОП для деплою.
