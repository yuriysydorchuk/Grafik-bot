# HANDOFF: візуальний поліш веб-панелі + закріплена шапка графіків + процес роботи

Сесія 2026-07-06…11. Все задеплоєно (коміти `2fb9a61`, `ea15e01`, `9a8fc8a`, `ba9c048`).
Довговічні правила вже перенесені в `CLAUDE.md`; тут — контекст сесії.

## Що зроблено

**Візуальний поліш панелі** (`9a8fc8a`) — лише спільні файли, розмітка/логіка сторінок не зачеплені:
- `web/src/components/ui.tsx`: кнопки (focus-visible-кільця, press-ефект, тінь на filled),
  модалки (анімація fade/pop-in, blur підложки, ring), інпути/селекти (hover-рамка),
  бейджі (inset-ring), Empty-стани (іконка Inbox).
- `web/src/index.css`: `--animate-fade-in`/`--animate-pop-in` у `@theme` (+keyframes);
  глобальний hover рядків таблиць у `@layer base` — utility-класи `bg-*` на рядках
  перекривають його (base < utilities), тож кольорові/вибрані рядки не ламаються.
- `web/src/components/Layout.tsx`: градієнтний аватар, тінь топбарів.
- Login: анімація появи картки (сторінка й так була оформлена).

**Закріплена шапка на /schedule** (`ba9c048`): перемикач тижнів + рядок
«фабрика/статус/дії» обгорнуті в `md:sticky md:top-[52px]` (52px = висота
десктопного топбара −1px перекриття; `-mx-8/px-8` розтягують непрозору смугу на
всю колонку контенту). **На мобілці свідомо не sticky** — кнопки переносяться на
2–3 рядки і зʼїли б екран. Смужка isFetching переїхала всередину шапки.

**Процес роботи** (правила вже в `CLAUDE.md`, тут для контексту):
- Схема «прочитай доки → знайди файли → план → чекай підтвердження» закріплена
  хуком `UserPromptSubmit` у `.claude/settings.json` (інжектить нагадування на
  кожне повідомлення) + розділ «Схема роботи над задачею» в CLAUDE.md.
- Правило опортуністичного дроблення `bot/index.ts` → `bot/handlers/` (виносити
  блок лише коли задача і так суттєво його зачіпає) — у «Правилах розробки».

**Оцінка Graphify** (knowledge-graph тул для Claude Code): вирішено **не ставити** —
проект замалий (~40 файлів), роль графа виконують CLAUDE.md/PROJECT_MAP.md;
переглянути, якщо візьмемось за великий рефакторинг `bot/index.ts` або репо виросте.

## Перевірено

- typecheck/build web, локальний смоук (сторінки 200, нові класи в бандлі), прод
  після кожного деплою (healthz ok, /login 200, CSS-бандл із новими правилами).

## Нюанси / TODO

- Зміни поліша **делікатні за задумом** — де дивитися: модалки (анімація), hover
  рядків таблиць, Tab-фокус кнопок, аватар у сайдбарі. Після деплою користувачу
  може знадобитися жорстке оновлення (Cmd+Shift+R) через кеш бандла.
- **Темний брендовий сайдбар** — запропонований, власник ще не вирішив; якщо
  захоче помітнішої зміни вигляду — робити окремим комітом для легкого відкату.
- Sticky-шапка на мобілці (наприклад, лише перемикач тижнів) — за запитом.
- Якщо колись зміниться висота десктопного топбара в Layout.tsx — оновити
  `md:top-[52px]` у Schedule.tsx.
