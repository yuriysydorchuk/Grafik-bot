# HANDOFF: email графіків клієнтам увімкнено (2026-07-07)

Одноразова записка сесії. Задеплоєно на прод комітами `c6719c5` та `9472b36`.

## Ітерація 2 (той самий день, коміт `9472b36`)

- **Формат листа змінено:** тіло — звичайний текст «Dzień dobry. Wysyłam grafik na
  {data}» + підпис (Viktoriia Oliinyk, контакти, NIP/Regon); графік — **лише**
  Excel-вкладенням (HTML-таблиці в тілі більше немає). Тема: `Grafik na {data}`,
  де {data} = `07.07.2026` (день) або `07.07.2026 – 13.07.2026` (тиждень).
- **Шаблон редагується:** Налаштування → «Email-шаблони» (гейт editData);
  зберігається в `settings` (`email_tpl_schedule_subject`/`_body`),
  плейсхолдери `{data}`, `{fabryka}`; кнопка «Скинути до стандартного».
  API: `GET/PUT /email-templates`. Дефолти — `SCHEDULE_EMAIL_DEFAULTS`
  у `services/email.ts` (порожній/нетронутий запис у БД = дефолт з коду).
- **Поденне затвердження:** на кожній картці дня в Графіках кнопка «Затвердити»
  → модалка з двома чекбоксами (обидва проставлені): «Розіслати працівникам у
  Telegram» (старий поденний notify, ставить sentAt) і «Надіслати на фабрику
  (email)» (одноденний Excel). Окремої кнопки «Розіслати» на дні більше немає.
  Тижневі кнопки зверху — без змін.

## Що зроблено

1. **SMTP підключено.** Відправник — Gmail `office.eurosupp@gmail.com` (app password,
   2FA увімкнена). Змінні `SMTP_HOST/PORT/USER/PASS/FROM` заповнені у `.env` локально
   та на проді (smtp.gmail.com:465). Пароль — **лише** в `.env`, у репо його немає.
   Ліміт Gmail ~500 листів/день — для графіків з запасом.
2. **Фікс бага у `services/email.ts`:** `sendScheduleEmail` фільтрував записи лише по
   фабриці, без тижня — лист збирав зміни з УСІХ тижнів. Тепер: тиждень резолвиться
   по `weekStart` (approved-пріоритет, як у `/schedule/approve`), фільтр по `weekId`
   + виключення `status='absent'` (дзеркалить Excel-експорт на Drive).
3. **Excel-вкладення.** До листа прикріплюється той самий файл, що йде на Drive
   (`buildScheduleExcelBuffer`, сегрегація посада/стать); для одного дня — одноденний файл.
4. **День або тиждень.** `sendScheduleEmail(factoryId, weekStart, day?)`; новий
   `POST /schedule/email` (гейт `editData`, body: `weekStart`, `factoryId`, опц. `day`).
   У веб-панелі на Графіках — кнопка **Email** (поруч із «Розіслати»): модалка
   «весь тиждень / конкретний день». Модалка затвердження без змін (opt-in чекбокс,
   шле тиждень). Бот-флоу затвердження — без змін (шле тиждень, тепер із вкладенням).
5. i18n: нові рядки uk+en (`web/src/lib/i18n.tsx`). Лист — польською (правило проєкту).

## Нюанси

- Веб-частина (кнопка Email + `EmailModal` у `Schedule.tsx`) поїхала раніше в коміті
  `9a8fc8a` (паралельна сесія підхопила її у свій «style»-коміт). Бекенд — `c6719c5`.
- Якщо на день/тиждень немає змін — лист **не** шлеться, повертається статус
  «немає змін» (щоб не слати клієнту порожні графіки).
- Падіння побудови Excel не блокує лист — піде без вкладення (залоговано error).
- OAuth-refresh-токени Drive тут ні до чого: пошта йде звичайним SMTP + app password;
  але app password вмирає при зміні пароля акаунта або вимкненні 2FA — тоді
  згенерувати новий і оновити `SMTP_PASS` у `.env` на проді + `pm2 restart grafik-bot`.

## Перевірено

- typecheck / 27 тестів / build — зелені.
- SMTP: тестовий лист з office.eurosupp@gmail.com доставлено (250 OK від gsmtp).
- Прод після деплою: `healthz` `{"status":"ok","db":"ok","bot":"up"}`, у логах лише
  штатний алерт «service started».

## Що лишилось зробити руками (власник)

- У веб-панелі → Фабрики: вписати реальні email клієнтів (зараз надсилання просто
  скаже «email клієнта не вказано»).
- Перший бойовий тест: поставити тестовій фабриці свою адресу, надіслати «конкретний
  день», перевірити лист+Excel, потім замінити на адресу клієнта.
