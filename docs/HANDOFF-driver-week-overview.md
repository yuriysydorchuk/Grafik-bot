# HANDOFF: «Графік тижня» головного водія — розбивка по фабриках

Сесія 2026-07-06…07. Задеплоєно на прод (коміт `bbccc44`), healthz ok.

## Контекст

Фідбек водія (Саня, скріншоти в Telegram): «📅 Графік тижня» в боті головного водія
показував повні списки людей і суму по зміні **без фабрик** («2 зміна (39 чел.)» —
все в кучу, незрозуміло скільки людей і на яку фабрику везти).

## Що зроблено

Одна точкова правка — `showFullWeekSchedule` у `artifacts/api-server/src/bot/views.ts`:

- групування **день → фабрика → зміна**, замість день → зміна по всіх фабриках разом;
- **лише кількість осіб** на слот, без списків імен (рішення власника: список людей
  доречний тільки у флоу посадки — `showDriverShift` не чіпали);
- біля слота — призначений водій із `driver_shift_assignments` (**фільтр `kind='delivery'`**,
  забори не показуються, дедуп імен через Set); слот без суфікса 🚗 = водія не призначено;
- нових i18n-рядків нема («Графік», «ос.» вже перекладені); `sendLongMessage` лишили
  (повідомлення тепер коротке, але фабрик може стати більше).

## Перевірено

- typecheck чистий, 27/27 тестів, esbuild-збірка ок.
- Прод: `{"status":"ok","db":"ok","bot":"up"}`, у логах лише штатний startup-алерт;
  новий код у бандлі на сервері (`grep driversOf dist/index.mjs`).
- Живе натискання кнопки головним водієм не перевірялося — попросити Саню глянути.

## TODO / не зроблено (свідомо)

- Перший пункт фідбеку Сані на скріншоті обрізаний («…как это отдельная поездка идет»,
  ймовірно про окрему поїздку/забір) — **уточнити у водія, що малося на увазі**.
- У локальному робочому дереві лишилися **чужі незакомічені зміни іншої сесії**
  (`routes/admin-api.ts`, `web/{Dashboard,NotificationBell,i18n}`) — у цей деплой не
  входили, сервер збирає з чистого `main`. Розібратися, чиї вони й чи потрібні.
