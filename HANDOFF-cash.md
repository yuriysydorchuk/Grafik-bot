# HANDOFF: Каса повністю на сайті (06.08.2026)

Записка сесії. Довговічне вже перенесено в `PROJECT_MAP.md` (маршрути `/cash`, cron, схема) — тут контекст, разові кроки і межі деплою.

## Що зроблено

Каса переведена з гугл-таблиці STAN KASY на сайт (`/cash`), таблиця **виведена з експлуатації** (щоденний синк прибраний з крону і з `POST /bank/sync`; `services/cashRegister.ts` лишився для разових скриптів, історичні рядки в БД — read-only).

- **Категорії** — власний довідник `cash_categories` (CRUD на /cash, веде кадрова): видатки + приходи. Зарплати розбиті по містах/типах (`payroll`+`city`): фабрики Люблін/Лодзь/Познань, офіс Люблін/Лодзь, Клінекс (прибирання) + legacy «без розбивки» для історії. «Повернення коштів працівникам» — `requires_desc` (опис обовʼязковий). Авто-класифікація старих описів — `services/cashCategories.ts` (під тестами).
- **Форма запису:** видаток — категорія обовʼязкова; прихід — «Знято з карти» / «Додатковий прихід» (+категорія: оплата хостела, карта побиту, дозволення, повернення від працівника, від власника, інше); `opening` = місячний перерахунок кадрової, тепер і для каси офісу по фірмі.
- **Звірки:** банк↔каса (лише приходи «з карти»); ЗП: каса K ↔ Σ gotowka сводної K−1 по містах/офісах/Клінексу; межі місяців (відкриття≠закриття). Розбіжності «фіксуються» з поясненням (`cash_recon_acks`) — зникають з алертів, лишаються в журналі. Поки сводна K−1 без готівкових сум — звірка ЗП мовчить (сіра примітка, не алярм). Зведений блок «Каса: розбіжності» — на Кешфлоу.
- UI: колонки Категорія|Опис (нотатка — другим рядком), модалка категорій сіткою, фіксація через модалку (не `window.prompt` — той блокується частиною браузерів).

## Разові кроки після деплою (Yuriy)

1. Внести **«Початковий залишок (перерахунок)»** на 1 серпня по кожній фірмі каси офісу (і по сейфах за потреби).
2. Сказати кадровій: каса тепер **тільки на сайті**; таблиця — архів.
3. Перевірити/докатегоризувати липневі-серпневі записи (авто-категорії можна поправити прямо в списку).
4. Тестові записи від 06.08 («вавіа», «віаві» — −2 020 і +840) — видалити або виправити.
5. Звірка ЗП оживе, щойно сводна липня матиме готівкові суми.

## Межі цього деплою (важливо)

Коміт зібраний **вибірково** (worktree від прод-HEAD): на прод поїхала лише каса + 1 міграція `2026-08-06-cash-categories.sql`. У локальному дереві лишився **недеплоєний батч** (папка водія: автопарк/транспорт/одяг, хостели 2.0, пальне Orlen, P&L по містах, фактури витрат, ролі fuel/hostelOps/costInvoices) з **10 ненакатаними міграціями** `2026-07-31-*`…`2026-08-03-*` — деплоїться окремо після сайн-офу.

Нюанси через це:
- Ящик **«Каса хостелів»** на проді видно, але він порожній (дані — з міграції папки водія).
- `OFFICE_TAB_RE` продубльований у `routes/cash.ts` (локальна const), бо `/pnl/actuals` з цим експортом ще не на проді — при деплої батчу можна звести до одного означення.
